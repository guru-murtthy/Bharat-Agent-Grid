# Copy to .env and fill in. NEVER commit real keys.
# If unset, the system runs in deterministic stub mode (no external calls).
LLM_PROVIDER=
OPENAI_API_KEY=
# Optional Indian-language speech provider
BHASHINI_API_KEY=
